events {}

http {
  server {
    listen 8080;

    location / {
      root /usr/share/nginx/html;
      index index.html;
    }

    location /order/ {
      proxy_pass https://api-gateway-service-suryadasari31-dev.apps.rm2.thpm.p1.openshiftapps.com;
      proxy_set_header Host $host;
    }
  }
}

